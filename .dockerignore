# Git and editor
.git
.gitignore
.cursor
.claude
.DS_Store

# Dependencies and local tooling (not used in the Go image)
node_modules/
package.json
package-lock.json
scripts/
!scripts/gen-asset-hashes.go
!scripts/docker-entrypoint.sh

# Runtime / local data (mounted as a volume in compose)
data/

# Local binaries
nextDash
main
*.exe
*.test

# Docs and assets not needed in the image
*.md
screenshots/
extension/
nextDash.code-workspace*

# Build artifacts
dist/
build/
.tmp/
.cache/

# Local environment secrets (Compose reads .env before building).
.env
.env.*
!.env.example
.tmp-*
